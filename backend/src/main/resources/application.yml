spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/impaintordb
    username: impaintor
    password: impaintor_pass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest